import styled from "styled-components";
import { px2rem } from '@/assets/global-style'

export const Wrapper = styled.div`
    padding: ${px2rem(160)} 0;
    text-align: center;
    font-size: ${px2rem(24)};
    color: #999;
`